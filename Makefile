prefix ?= ~/.local

bindir ?= $(prefix)/bin
libdir ?= $(prefix)/lib
mandir ?= $(prefix)/share/man
sharedir ?=  $(prefix)/share/iptables-firewall

all:

install:

uninstall:

clean:

distclean: clean

.PHONY: all install clean distclean uninstall

